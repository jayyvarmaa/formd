version: '3.8'

services:
  formd:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    container_name: formd
    restart: unless-stopped
    environment:
      - NGINX_HOST=localhost
      - NGINX_PORT=80
